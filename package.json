{
  "name": "digital-twin-viewer",
  "private": true,
  "version": "0.1.0",
  "author": "Alduino Calderon",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx cpx src/embedded.html dist/",
    "preview": "vite preview",
    "deploy": "npm run build && git add docs && git commit -m \"deploy\" && git push"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "mqtt": "^5.12.0",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1",
    "three": "^0.175.0"
  }
}
